import styled from 'styled-components';

const VideoStyled = styled.video`
  width: 100%;
  height: 200px;
`;
const ButtonsWrapper = styled.div`
  display: flex;
  justify-content: flex-start;
  width: 100%;
`;

export { VideoStyled, ButtonsWrapper };
